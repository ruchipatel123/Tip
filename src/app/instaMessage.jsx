import React from "react";

export default function InstagramMessage() {
 
  return (
    <section
      style={{
        fontFamily:
          '-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif',
        padding: "20px",
        maxWidth: "400px",
        margin: "0 auto",
      }}
      className="bg-white"
    >
      Grazie per aver scelto Traininpink™!
    </section>
  );
}
